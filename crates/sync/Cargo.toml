[package]
name = "cairn-sync"
description = "federated sync with vector clocks for cairn"
license = "MIT"
repository = "https://github.com/valtors/cairn"
version = "0.1.1"
edition = "2021"

[dependencies]
cairn-store = { path = "../store", version = "0.1.1" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
